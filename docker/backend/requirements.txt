# ─────────────────────────────────────────────────────────────────────────────
# Consolidated requirements for the unified backend container.
# No Redis, no RabbitMQ, no supervisor — all replaced with in-process state.
# ─────────────────────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ── In-memory caching (replaces Redis) ────────────────────────────────────────
cachetools>=5.3.0

# ── Object storage ────────────────────────────────────────────────────────────
aioboto3>=13.0.0

# ── Vector database client ────────────────────────────────────────────────────
pgvector>=0.3.0

# ── Document & image processing ───────────────────────────────────────────────
PyMuPDF>=1.24.0
Pillow>=10.2.0

# ── HTTP clients ──────────────────────────────────────────────────────────────
httpx>=0.27.0
aiohttp>=3.9.0
requests>=2.31.0

# ── NLP / tokenisation ────────────────────────────────────────────────────────
transformers>=4.40.0
tokenizers>=0.19.0
sentencepiece>=0.2.0

# ── ONNX inference ────────────────────────────────────────────────────────────
onnxruntime>=1.18.0
numpy>=1.26.0

# ── ONNX model export / quantisation (model-init) ─────────────────────────────
optimum[onnxruntime]>=1.20.0
torch>=2.3.0
protobuf>=4.25.0

# ── Sparse retrieval ──────────────────────────────────────────────────────────
rank-bm25>=0.2.2

# ── Auth ──────────────────────────────────────────────────────────────────────
PyJWT>=2.8.0
python-dotenv>=1.0.1

# ── OpenAI Vision (inline OCR) ────────────────────────────────────────────────
openai>=1.52.0

# ── Database ──────────────────────────────────────────────────────────────────
asyncpg>=0.29.0

# ── Observability ─────────────────────────────────────────────────────────────
prometheus_client>=0.20.0
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-instrumentation-httpx>=0.45b0
